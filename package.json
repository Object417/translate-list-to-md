{
  "name": "translate-list-to-md",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite serve --open",
    "dev": "vite server",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
